#ifndef GLOBALS_H
#define GLOBALS_H

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <ctype.h>

extern FILE *mon_fichier;
extern char curr_char;

extern char curr_tag[81];
extern char tag_value[81];

#endif